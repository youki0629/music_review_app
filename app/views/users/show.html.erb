<div class="container mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-6">マイページ</h1>
  
  <!-- ユーザー情報 -->
  <div class="bg-white shadow rounded-lg p-6 mb-8">
    <h2 class="text-lg font-semibold mb-4">ユーザー情報</h2>
    <p><strong>メールアドレス:</strong> <%= @user.email %></p>
    <p><strong>登録日:</strong> <%= @user.created_at.strftime("%Y年%m月%d日") %></p>
  </div>
  
  <!-- レビュー一覧 -->
  <div class="bg-white shadow rounded-lg p-6">
    <h2 class="text-lg font-semibold mb-4">投稿したレビュー (<%= @reviews.count %>件)</h2>
    
    <% if @reviews.any? %>
      <div class="space-y-4">
        <% @reviews.each do |review| %>
          <div class="border border-gray-200 rounded-lg p-4">
            <h3 class="font-semibold text-lg"><%= review.title %></h3>
            <p class="text-gray-600 text-sm">by <%= review.artist %></p>
            <p class="text-gray-500 text-sm">ジャンル: <%= review.genre %></p>
            <p class="text-gray-400 text-xs mt-2">
              投稿日: <%= review.created_at.strftime("%Y年%m月%d日 %H:%M") %>
            </p>
            <% if review.body.present? %>
              <div class="mt-3">
                <p class="text-gray-700"><%= simple_format(review.body) %></p>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-8">
        <p class="text-gray-500 mb-4">まだレビューを投稿していません。</p>
        <%= link_to "レビューを投稿する", new_review_path, 
            class: "bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded" %>
      </div>
    <% end %>
  </div>
  
  <!-- ナビゲーション -->
  <div class="mt-8 text-center">
    <%= link_to "レビュー一覧に戻る", reviews_path, 
        class: "text-blue-600 hover:underline mr-4" %>
    <%= link_to "新しいレビューを投稿", new_review_path, 
        class: "bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded" %>
  </div>
</div>